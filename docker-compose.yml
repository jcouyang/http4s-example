version: '3.4'

services:
  zipkin:
    image: openzipkin/zipkin
    ports:
      - 9411:9411
  db:
    image: postgres:10
    # volumes:
      # - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=joke
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
volumes:
  db-data:
